from setuptools import setup

setup(name='dist_uploadTest_exercise',
      version='0.1',
      description='Gaussian distributions, Binomial distributions',
      packages=['dist_uploadTest_exercise'],
      zip_safe=False)
