from setuptools import setup

setup(name='BGGDistributions',
      version='0.1',
      description='Gaussian, Binomail, and General distributions',
      packages=['BGGDistributions'],
      zip_safe=False)
