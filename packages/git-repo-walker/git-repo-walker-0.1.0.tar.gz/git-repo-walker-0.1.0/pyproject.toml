[tool.poetry]
name = "git-repo-walker"
version = "0.1.0"
description = ""
authors = ["Daniel Elsner <daniel.elsner@tum.de>"]

[tool.poetry.dependencies]
python = "^3.7"
gitpython = "^3.1.2"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[tool.poetry.scripts]
repo-walker = "examples.cli:main"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
