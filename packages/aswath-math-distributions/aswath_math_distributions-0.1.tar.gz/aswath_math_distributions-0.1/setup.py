from setuptools import setup

setup(name='aswath_math_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['aswath_math_distributions'],
      zip_safe=False)
