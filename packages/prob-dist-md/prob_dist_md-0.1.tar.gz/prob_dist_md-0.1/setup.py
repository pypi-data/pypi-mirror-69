from setuptools import setup

setup(name='prob_dist_md',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['prob_dist_md'],
      author = 'Michael Diggin',
      zip_safe=False)
