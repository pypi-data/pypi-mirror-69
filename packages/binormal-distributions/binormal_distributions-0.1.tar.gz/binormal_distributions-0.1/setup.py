from setuptools import setup

setup(name='binormal_distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['binormal_distributions'],
      author = 'Tharun Ragulan',
      zip_safe=False)
