from setuptools import setup

setup(name='distributions-by-mj',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['distributions-by-mj'],
      zip_safe=False)
