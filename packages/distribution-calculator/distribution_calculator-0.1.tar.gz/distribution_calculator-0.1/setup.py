from setuptools import setup

setup(name='distribution_calculator',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distribution_calculator'],
      author = 'Sumeet Sarode',
      zip_safe=False)
