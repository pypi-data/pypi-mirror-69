from setuptools import setup

setup(
    name="PyFiler",
    version="1.3.0",
    description="A file wrapper and API for python, supports most common files.",
    packages=["pyfiler"],
    install_requires=[
        "pysheets"
    ]
)
