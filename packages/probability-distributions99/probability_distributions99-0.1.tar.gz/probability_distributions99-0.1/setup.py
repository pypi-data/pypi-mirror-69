from setuptools import setup

setup(name='probability_distributions99',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['probability_distributions99'],
      zip_safe=False)
