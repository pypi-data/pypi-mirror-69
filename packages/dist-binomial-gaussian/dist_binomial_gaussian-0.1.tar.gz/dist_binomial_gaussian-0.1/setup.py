from setuptools import setup

setup(name='dist_binomial_gaussian',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      author = 'Chanakarn Luangpirom',
      zip_safe=False)
