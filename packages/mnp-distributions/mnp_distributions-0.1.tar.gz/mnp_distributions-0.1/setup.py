from setuptools import setup

setup(name='mnp_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['mnp_distributions'],
      zip_safe=False)
