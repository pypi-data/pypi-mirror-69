.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Basic SFTP, run this command in your terminal:

.. code-block:: console

    $ pip install basic_sftp

This is the preferred method to install Basic SFTP, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Basic SFTP can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/blegarth/basic_sftp

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/blegarth/basic_sftp/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/blegarth/basic_sftp
.. _tarball: https://github.com/blegarth/basic_sftp/tarball/master
