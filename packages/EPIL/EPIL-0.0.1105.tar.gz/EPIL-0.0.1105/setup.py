from setuptools import setup

ldesc = 'Module for work with PIL (replace colors, change color and more...)'
desc  = 'Module for work with PIL (replace colors, change color and more...)'

setup (

    name='EPIL',
    version='0.0.1105',
    description=desc,
    long_description=ldesc,
    packages=['EPIL'],
    author_email='kolyaleonof@yandex.ru'

)
