[tool.poetry]
name = "arcane-core"
version = "0.5.2"
description = "Common utility functions"
authors = ["Arcane <product@arcane.run>"]
packages = [
    { include = "arcane" }
]

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2.23.0"
backoff = "^1.10.0"
arcane-firebase = "0.1.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
