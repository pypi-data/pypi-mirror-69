from setuptools import setup

setup(name='Frank_distributions',
      version='2.7',
      description='Gaussian and Binomial distributions',
      packages=['distributions'],
      author = 'Frank',
#       author_email = 
      zip_safe=False)
