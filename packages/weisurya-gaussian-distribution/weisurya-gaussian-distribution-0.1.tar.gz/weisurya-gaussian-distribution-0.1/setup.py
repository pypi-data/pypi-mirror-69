from setuptools import setup

setup(name='weisurya-gaussian-distribution',
      version='0.1',
      description='Gaussian distributions by weisurya',
      packages=['distributions'],
      zip_safe=False)
