from setuptools import setup

setup(name='aayush_distributions',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['aayush_distributions'],
      zip_safe=False)
