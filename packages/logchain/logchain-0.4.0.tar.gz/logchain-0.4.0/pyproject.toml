[tool.poetry]
name = "logchain"
version = "0.4.0" # Placeholder updated by `poetry version $VERSION`
description = "Python Logging secured by blockchain"

authors = [ "Gg Math <code@gmath.io>" ]
license = "ISC"

readme     = "README.md"
repository = "https://gitlab.com/ggpack/logchain"
homepage   = "https://gitlab.com/ggpack/logchain"

keywords = ["logging", "security", "blockchain", "signature"]

[tool.poetry.dependencies]
python = "^3.2"
