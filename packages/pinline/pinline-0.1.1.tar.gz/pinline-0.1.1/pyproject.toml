[tool.poetry]
name = "pinline"
version = "0.1.1"
description = ""
authors = ["Simon Perkins <simon.perkins@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
pytest = {version = "^5.4.2", optional = true}
pytest-flake8 = {version = "^1.0.6", optional = true}
unparse = {version = "^0.2.0", optional = true}
astunparse = {version = "^1.6.3", optional = true}

[tool.poetry.dev-dependencies]
pytest = "^5.2"
bump2version = "^1.0.0"

[tool.poetry.extras]
testing = ["astunparse", "pytest", "pytest-flake8"]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
