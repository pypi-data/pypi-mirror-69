from setuptools import setup

setup(name='GaB_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      author = 'Khondoker',
      packages=['GaB_distributions'],
      zip_safe=False)
