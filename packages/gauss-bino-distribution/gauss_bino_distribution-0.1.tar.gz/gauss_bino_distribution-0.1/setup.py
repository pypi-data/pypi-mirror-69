from setuptools import setup

setup(name='gauss_bino_distribution',
      version='0.1',
      description='Gaussian and Binomial Distributions',
      packages=['gauss_bino_distribution'],
      zip_safe=False)
