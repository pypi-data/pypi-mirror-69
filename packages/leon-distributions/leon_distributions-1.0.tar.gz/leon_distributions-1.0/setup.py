from setuptools import setup

setup(name='leon_distributions',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['leon_distributions'],
      author='Panayiotis Leontiou',
      zip_safe=False)
