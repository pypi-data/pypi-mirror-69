from setuptools import setup

setup(name='stat_distributions_prob',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['stat_distributions_prob'],
      author ='Philip Wong',
      zip_safe=False)
