from setuptools import setup

setup(name='mdev_gaussian_binomial_distributions',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['mdev_gaussian_binomial_distributions'],
      zip_safe=False)
