[tool.poetry]
name = "sentipy"
version = "0.1.0"
description = ""
authors = ["UpstatePedro"]

[tool.poetry.dependencies]
python = "^3.6"
numpy = "^1.18.4"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
