from setuptools import setup

setup(name='udacity_aws_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['udacity_aws_distributions'],
      zip_safe=False)
