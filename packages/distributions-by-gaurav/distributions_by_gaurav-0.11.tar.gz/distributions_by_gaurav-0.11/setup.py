from setuptools import setup

setup(name='distributions_by_gaurav',
      version='0.11',
      description='Gaussian distributions, Binomial distributions',
      packages=['distributions_by_gaurav'],
      zip_safe=False)
