from setuptools import setup

setup(name='My_binomial_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['My_binomial_distributions'],
      zip_safe=False)
