from setuptools import setup

setup(name='basic_probability',
      version='0.2',
      description='Library for basic operations on Binomial and Gaussian distributions',
      packages=['basic_probability'],
      zip_safe=False)
