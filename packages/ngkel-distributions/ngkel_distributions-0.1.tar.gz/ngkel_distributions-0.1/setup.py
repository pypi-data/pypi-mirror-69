from setuptools import setup

setup(name='ngkel_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['ngkel_distributions'],
      authoer = 'Ng Ka Lok',
      author_email = 'ngkel@connect.hku.hk',
      zip_safe=False)
